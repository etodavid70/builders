import 'package:flutter/material.dart';



// Define your reusable colors as constant values
const Color primaryColor = Color.fromRGBO(24, 101, 51, 1);
const Color primaryColorFaint = Color.fromRGBO(24, 101, 51, 0.5);
const Color secondaryColor = Colors.white; // Orange shade
const Color backgroundColor = Color(0xFFF7F7F7); // Light gray shade

// Optionally, you can define additional colors
const Color textColor = Colors.black;
const Color disabledColor = Colors.grey;


